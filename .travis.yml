sudo: required

language: php

services:
  - docker

before_install:
  - docker-compose build
  - docker-compose up -d
  - docker-compose ps
  - docker-compose exec workspace bash

script:
  - composer install
  - ./vendor/bin/codecept run
